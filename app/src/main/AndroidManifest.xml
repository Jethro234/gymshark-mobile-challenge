<?xml version="1.0" encoding="utf-8"?>
<!--
  Launcher icon is the system placeholder: docs/DESIGN.md specifies no
  launcher artwork. Swap for a real adaptive icon once one is designed.

  The launcher activity, NavDisplay wiring and theme are added in task 9
  (openspec/changes/build-product-catalogue/tasks.md group 9) alongside
  Navigation 3.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".GymsharkApplication"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

    </application>

</manifest>
